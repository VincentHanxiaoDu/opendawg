// Re-export from @opendawg/channel-core — Discord uses string user IDs, matching the core.
export {
    ServerRegistry,
    type ServerRecord,
    type DefaultServer,
} from "@opendawg/channel-core";
